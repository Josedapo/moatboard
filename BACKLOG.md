# Moatboard — Engineering backlog

**Status:** 2026-04-22 — Moatboard entra en uso real. Joseda empieza a analizar e invertir con el producto. Este archivo captura lo pendiente para que no se pierda; la prioridad la da el dogfooding, no la lista.

**Cómo se usa:**
- Cuando algo aparece durante el uso real, va arriba del todo en **Observed during use** con fecha.
- El resto son hilos documentados pero deferred — se pican cuando haya masa crítica o el uso lo demande.
- Mover items entre secciones es libre; cada entrada es una línea + una de contexto.

---

## Observed during use

_Aquí aterrizan las cosas que surjan al analizar / invertir de verdad. Fecha + una línea. Sin ordenar por prioridad — eso se resuelve cuando se decida retomar el backlog._

- **2026-04-28 · Watchlist como tag + kill del Decision step + pantalla Comprar dedicada (LOCAL).** Refactor conceptual: cartera y watchlist se modelaban como estados mutuamente excluyentes; no lo son. Cartera deriva de `positions` con net>0 (hecho binario derivable). Watchlist pasa a tag puro (estrella, sin `reason_md`). `discarded` y `outside_circle` colapsan como conceptos — cerrar el wizard sin comprar es la nueva forma de "descartar"; no hace falta registrar un "no" explícito. **Schema:** `ticker_states` → `watchlist_entries` (drop status / reason_md / review_when / prior_reason_on_invest_md); `analysis_sessions` pierde `outcome` + `completed_at` (sesiones eternales-resumibles, una fila por (user, ticker)); CHECK del step elimina `'decision'`. Migrations aplicadas: `migrate-watchlist-tags.mjs` (12 discarded borradas, 7 watchlist conservadas, 21 sesiones remapeadas) + `cleanup-analysis-sessions.mjs` (3 duplicados (user, ticker) dedupedeados, drop columnas + partial index → full unique index). **Wizard:** 4 pasos lineales (Quality → Understanding → Red flags → Valoración), sin Decision step. ★ `WatchlistStarToggle` (optimistic UI) en cabecera de `WizardShell`. Botón "Comprar" aparece solo al final de StepValuation. "Cerrar análisis" reemplaza los antiguos "Saltar a la decisión" / "No lo entiendo". `upsertPreAnalysisFromExisting` se hookea en cada step boundary para que tickers parcialmente analizados entren al scope de Iris. **Nueva pantalla `/dashboard/comprar/[ticker]`:** form precio + shares + fecha + `pre_commitment_md` (**obligatorio en primera compra**, opcional `operation_note` en adiciones) + checkbox opt-in "añadir a watchlist". `recordBuyTransactionAction` promueve draft, congela snapshot transaccional, redirige a `/position/[id]`. Reachable desde StepValuation, Decisión tab del ficha unificada y nueva inline `AddPositionForm` en cabecera de Cartera. **Discovery leaderboard:** estrella inline con el ticker; columna Estado → Análisis (binario: Analizada / No analizada por `business_tier_source === 'user'`); filtros all/unseen/watchlist; soft-dim para filas analizadas. **DecisionPanel:** 4 modos mutuamente excluyentes → 2 flags ortogonales (isOwned + isOnWatchlist). Tres acciones: Comprar, toggle watchlist, Re-analizar. **Cleanup:** `/dashboard/history` + nav "Descartes" eliminados; `DecisionContextStrip` + `lib/positionContext` (solo se disparaban para filas legacy con `outcome='invested'`); `decideInvestAction` / `decideWatchlistAction` / `decideDiscardAction` / `markOutsideCircleAction` / `moveTo*` server actions borradas; prior-state reminder + Re-analyze-anyway de AnalyzeEntryForm; auto-flip a discarded en sell-to-zero (filtro net>0 hace el trabajo); `restore-orphan-analyses.ts` y `canonicalize-ticker-states.mjs`. Verificación: tsc clean + build ✓.

- **2026-04-28 · Pestaña "Decisión" en la ficha + eliminación de botones "Re-analizar" (LOCAL).** Cierre lógico del cleanup: tras eliminar los Regenerar, los Re-analizar quedaban como segundo elemento redundante por toda la UI (top de la ficha en cualquier modo, fila en lista de watchlist, fila en lista de history descartadas). Funcionalmente re-walkear el wizard ya no aporta refresh de datos (Iris); lo único que aportaba era reconsiderar la decisión, así que se centraliza en una pestaña nueva. **Cambio:** nuevo `DecisionPanel.tsx` Server Component + tab `decision` en `PositionTabs` (orden: Overview · Calidad · Negocio · Valoración · Decisión · Señales). Por estado: `watchlist` muestra "Mover a cartera" + "Descartar"; `discarded` muestra "Reconsiderar · mover a cartera" + "Mover a watchlist"; `in_portfolio` muestra "Mover a watchlist" + "Descartar" (gateadas a netShares ≤ 0 con hint que dirige a Overview); `discovery` puro sin análisis muestra CTA "Empezar análisis →"; con análisis muestra las tres acciones. Forms inline expandidos con `<details>` (sin estado cliente). Tres server actions nuevas en `app/dashboard/ticker/[symbol]/actions.ts`: `moveToWatchlistAction`, `moveToDiscardedAction`, `moveToPortfolioAction` (registra buy + crea snapshot + flip ticker_state, reutiliza patrón de `decideInvestAction` del wizard pero sin requerir sesión activa). **UI removed:** botón Re-analizar del top de la ficha (todos los modos), fila Re-analyze de `/dashboard/watchlist`, fila Re-analyze de `/dashboard/history` (la branch wasHeld+notHeld colapsa en un único Link `Abrir ficha →` / `Reconsiderar →`). NotAnalyzedBlock dentro de la ficha (sub-bloques Calidad/Valoración/Negocio sin analizar) mantiene su CTA "Analizar →" — sigue siendo entry point legítimo para primera-vez. Discovery leaderboard / fund detail / new entrants también lo mantienen — entry CTAs. Verificación: tsc clean + build ✓.

- **2026-04-28 · Botones "Regenerar" eliminados — Iris extendido a valuations (LOCAL).** Tras la sesión anterior que mató los chats IA por gasto, surgió la pregunta natural: si Iris ya refresca Quality + Moat + Red flags + Understanding en 10-K nuevo y scorecard en 10-Q nuevo, ¿qué sentido tienen los 4 botones de regenerar manual en la ficha (Negocio understanding + stale banner, Red flags accordion, Calidad scorecard, Valoración)? Los tres primeros eran 100% redundantes. Valoración era la excepción: Iris no tocaba `valuations` y los anchors históricos (median multiple, FCF TTM) se quedaban congelados hasta regen manual. **Cambio:** nuevo helper `refreshValuationOnly(ticker)` en `lib/preAnalysisFlow.ts` que para cada `position` con canonical=ticker re-ejecuta `computeAndSaveValuation` (carry-forward de overrides de growth/multiple). Hooked en `processPreAnalysisForTicker` (10-K, ~1× IA/año vía guide TTL 365d) y `refreshScorecardOnly` (10-Q, **0 IA** — guide hits cache, scorecard ya recomputado, FCF TTM straight from SEC cache). **UI:** borrados los 4 botones + el stale banner (innecesario si Iris cumple). Wizard StepUnderstanding/StepRedFlags también limpiados para consistencia. Server actions retiradas: `regenerateUnderstandingAction`, `regenerateRedFlagsAction`, `runAnalysisAction`, `runValuationAction`. Iris log Spanish narration + metadata extendidos con `valuations_refreshed: N` ("Valoración recalculada en N fichas"). Si en futuro hace falta forzar regen tras un cambio de prompt, scripts CLI puntuales (`regenerate-valuations.ts` ya existe; agregar wrappers análogos para understanding/redflags si surge la necesidad). Verificación: tsc clean + build ✓.

- **2026-04-28 · Ficha unificada en `/dashboard/ticker/[symbol]` shipped (LOCAL).** Antes era dispatcher + tres rutas separadas (`/position/[id]`, `/watchlist/[ticker]`, wizard) cada una con su propio render de tabs. Ahora es una única ficha canónica con shell común y overview swappable por estado (`in_portfolio` → operations log + cost basis + Compromiso de salida; `watchlist` / `discarded` / `discovery` → próxima presentación + razón_md cuando hay + Funds holding card, sin footer "Re-analizar"). Tabs **Negocio / Calidad / Valoración / Presentaciones** idénticos en los 4 estados. La pestaña **Calidad** cae a `discovery_pre_analyses` cuando no hay `moatboard_analyses` per-user (cache compartido cruzando usuarios — ya implementado). `/position/[id]` y `/watchlist/[ticker]` legacy redirigen al canónico vía 308. Inbound Links de Dashboard / Watchlist list / History / UpcomingEarnings actualizados a `/dashboard/ticker/{ticker}` directo. Trajectory sigue en `/position/[id]/trajectory` (sub-route position-id-driven). El bug de "BRK-A descartada se renderizaba como Cartera" queda resuelto: ahora cualquier descartada usa el overview simple coherente con Watchlist.

- **2026-04-28 · Auto-refresh per-user tras filings shipped (LOCAL).** Cierra los dos gaps que la sesión de hoy identificó al revisar el sistema de refresh: (1) **10-Q tier propagation** — `refreshScorecardOnly(ticker)` en `lib/preAnalysisFlow.ts` ahora, después de actualizar shared cache `discovery_pre_analyses`, llama `propagateAnalysisToPerUserRows(canonical, analysis)` que encuentra todas las `positions` cuyo canonical matchea (cubre cartera/watchlist/descartada vía `LEFT JOIN ticker_aliases`) y para cada una con `moatboard_analyses` row existente, llama `saveAnalysis()` upsert con tier + scorecard_summary + moat_strength + moat_archetype + verdict_reason fresh. La prosa se reusa de `runAnalysis` que ya pagó IA para el shared cache — **cero coste IA adicional**. Skipea positions sin `moatboard_analyses` para no crear veredictos que el user nunca pidió. (2) **10-K business_understanding regen** — `processPreAnalysisForTicker(ticker)` ahora, tras red flags regen, llama `refreshUnderstandingIfStale()` que comprueba si la fila cacheada está desactualizada (mismo helper `isBusinessUnderstandingStale` que la UI) y si lo está, ejecuta `generateBusinessUnderstanding` + `saveNewUnderstanding` (archiva la previa, crea versión nueva). **Skipea tickers sin understanding cacheada** — la generación es opt-in vía wizard, no creamos contenido para tickers que nadie pidió. Coste IA: ~1 call/ticker/año (10-Ks anuales), bounded a ~50-200/año en el techo del universo. Verificación: tsc clean + DB inspection mostró 4 tickers (AAPL/AMZN/AXP/BAC) con rows que se propagarían y 1 understanding cacheado que se refrescaría en su próximo 10-K.

- **2026-04-28 · Pendiente: elevar `valuations` a render compartido cross-user.** Hoy la tabla `valuations` está keyed por `position_id` (per-user). Las otras piezas que conducen el Implied Return calculator (SEC fundamentals, quote, AI valuation guide, sustainable growth) ya son shared/deterministas. La ficha unificada del 2026-04-28 dejó como placeholder "Valoración sin calcular · Analizar →" para los estados sin position (Discovery puro / Discarded sin valuation propia). **Plan**: extraer `computeImpliedReturnEphemeral(ticker, quote, fundamentals, qualityTier?)` que ejecute el mismo math sin tocar DB, devolver una `Valuation` shape con `position_id: -1`. La ficha Discovery puro renderiza el calculator entero en read-only; los lápices ✎ del override de multiple/growth se reemplazan por CTA "Analizar para personalizar". Per-user (`fundamentals_snapshots` para Evolución, overrides editables, persistencia para regeneraciones futuras) sigue requiriendo position+wizard. Coste IA: cero adicional — todos los inputs ya están cacheados shared. Scope: ~2-3h (refactor de `computeAndSaveValuation` para extraer helper puro + flag `ephemeral`/`readOnly` en `ImpliedReturnCalculator`).

- **2026-04-28 · Agent observatory eliminado, modelo de cache compartido per-ticker shipped.** El cron semanal de pre-tiering masivo (272 candidatos × moat IA + red flags IA + 10-K download) consumió todos los tokens de la cuenta Max de Joseda en una corrida con apenas 46 tickers procesados. Pivoted: la tabla `discovery_pre_analyses` se conserva como **shared cache per-ticker** (modelo "analizar una vez, beneficio para todos los usuarios"), pero ya no se llena por cron masivo. Tres cambios principales:
  1. **Borrado del cron masivo y batch.** `/api/cron/pre-analysis`, `vercel.json` schedule `0 8 * * 2`, `runDiscoveryPreAnalysisJob`, `scripts/warm-sec-cache.ts`, `scripts/backfill-discovery-pre-analysis.ts`. Removido panel `DiscoveryAgentHeartbeat`, lib `preAnalysisStats`, sufijo "·agente" del `BusinessTierChip`, filtro "Sin tier aún", marca `business_tier_source` en leaderboard query.
  2. **Hook user → shared cache** en `advanceStepAction(ticker, 'valuation'|'decision')` y `regenerateRedFlagsAction`: nueva `upsertPreAnalysisFromExisting(ticker)` (lib/preAnalysisFlow.ts) lee `moatboard_analyses` + `moat_assessments` + `qualitative_red_flags` + `sec_fundamentals_cache` ya escritos durante el wizard y los lifta al row global. Cero IA — coste cero. Idempotente.
  3. **Re-evaluación automática event-driven.** Cuando el cron diario de SEC signals detecta un **10-K nuevo** (`form === '10-K'`, no amendment) para un ticker con row en `discovery_pre_analyses`, dispara `processPreAnalysisForTicker(ticker)` post-loop (1 vez por ticker, no per-user). Refresca Quality + Moat (moat AI se invalida por accession via `isMoatStale`) + Red flags. Para **10-Q nuevo**, `ensureQuarterlySnapshots` invoca `refreshScorecardOnly(ticker)` que recomputa scorecard + tier desde fundamentals frescos sin tocar IA (Item 1A vive en 10-K, no en 10-Q; moat se mantiene del último 10-K). Coste amortizado al universo de usuarios: ~1 ciclo IA por ticker por año, no por usuario.
  Los 14 tickers ya pre-analizados ayer se conservan tal cual — análisis válido, cualquier usuario los ve. Los rows con `tier_preset` siguen escritos a `moatboard_default`; el split CAPA 1 / CAPA 2 del refactor 683d420 sigue intacto para futuros presets.

- **2026-04-23 · Local mode JSON-in-text fallback for Claude Agent SDK.** Los 2 typed callers (`businessUnderstandingAi`, `redFlagsAi`) ahora envían el schema inline en el prompt y parsean la salida de texto de Opus. Smoke-test en V dio JSON válido a la primera. Si en dogfooding Opus empieza a fallar parsos de forma recurrente (unescaped quotes, newlines sin escapar), la opción B es migrar a `@anthropic-ai/claude-agent-sdk` cuyo `query()` acepta tools custom con schemas typed manteniendo auth Max. ~2h de trabajo. Mantener como plan B documentado; no tocar mientras Opus cumpla.

- **2026-04-27 · Base case Implied Return — primary multiple + min(current, median) + UI shipped (LOCAL).** Cerrada la primera mitad de la entrada del 2026-04-25. Tres cambios juntos en una sesión: **(1)** El múltiplo que conduce el implied return ya no está hardcodeado a P/E. Ahora `selectPrimaryMultipleSnapshot` (lib/multipleSelection.ts) lee el `primary_tool` de la AI valuation guide; mapea `pe`/`pfcf`/`pb` al snapshot correspondiente; cae a dispatch determinístico por business type cuando la guide no recomienda multiple (`dcf`/`cash_yield`) o no está disponible (balance-sheet → P/B, REIT → P/FCF, product → P/FCF). El P/FCF se deriva invirtiendo el snapshot de yield (1/yield) — caveat documentado: median(1/x) ≠ 1/median(x), aceptado pragmáticamente para v1. **(2)** Base case dejó de ser "current held constant" (multipleChangeBase = 0). Nueva regla en `deriveMultipleChangeBase`: si current ≤ median del propio histórico, mantiene current (no asume re-rating al alza, evita doble-conteo de la barateza ya capturada en FCF Yield); si current > median, comprime a la mediana sobre 10y. Es la disciplina Smith/Akre/Sleep operativa. Stress sin cambios (sigue Q1). **(3)** UI del `ImpliedReturnCalculator` muestra el múltiplo terminal real en cada escenario (`27.5x` mediana 10y / `Nx` Q1 hist.) con el `±X%/año` como subordinate. Sección Detalles cita los números reales y explica de dónde sale el múltiplo (AI guide vs deterministic). Persistencia: `multiple_label`, `multiple_source`, `multiple_current/median/q1`, `multiple_base/stress_terminal` en `assumptions`. ensureValuationGuide se adelantó dentro de `computeAndSaveValuation` (caché golpea en cold path después). 17 valuations regeneradas: GOOGL/AAPL/MSFT comprimen como esperaba; V queda comprable manteniendo current; KNSL sigue pasando comprable porque la mediana propia 7.4x P/B refleja régimen anómalo — sigue pendiente la otra mitad de esta entrada (cross-sectional anchor + override editable abajo).

- **2026-04-27 · Cross-sectional anchor + disclaimer + override editable shipped (LOCAL — Route A).** Las tres sub-piezas de la entrada del 2026-04-25 cerradas en una sola sesión vía Route A (hardcoded Damodaran sectors). **(1) Peer median table** en `lib/peerMedians.ts` — ~50 industries con `" - "` (yfinance separator) + sector fallback. Sources: Damodaran 2025 + clasificación manual para Moatboard tier-A peers. `getPeerMedian({ sector, industry, multipleLabel })` → `{ value, source: 'industry'|'sector' } | null`. **(2) Disclaimer card** en `ImpliedReturnCalculator.tsx` ZONE 1 — gatilla cuando `current/peer ≥ 1.5×`. Texto explícito citando los números reales + dirige al botón ✎ del override. Solo informativa, no afecta verdict. **(3) Override editable** vía `MultipleRowEditable.tsx` (Client) + nueva `updateImpliedReturnOverrideAction` server action. UI: pencil button por celda → input Nx + Save/Reset/Cancel. Persistencia en `multiple_change_*_override` (audit trail). Carry-forward en `computeAndSaveValuation` para que regeneraciones no pisen ediciones manuales. Smoke test 17 valuations: KNSL 4.1x P/B vs peer 1.5x = 2.73× → disclaimer ✓; AAPL/GOOGL/MSFT también disparan; V/META/INTU/BAC/MCO no. Bug encontrado y arreglado mid-sesión: las claves originales usaban em-dash `—` mientras yfinance usa `" - "` (espacio-guión-espacio); todo el industry-tier era dead lookup hasta el fix.

- **2026-04-25 · Cross-sectional anchor — Route B + C diferidas a futuro.** Route A (hardcoded) shipped 2026-04-27 cubre el v1 del problema KNSL. Lo que queda como mejora futura: **Route B (Discovery-cached peer medians)** — tabla `discovery_peer_medians` poblada por cron semanal sobre los ~860 tickers del Discovery roster, agrupados por business_type. Más preciso, dinámico, captura regime shifts. ~3-4h trabajo. Reservado para cuando aparezca segundo caso testigo distinto de KNSL donde la tabla hardcoded se quede corta. **Route C (Damodaran annual ingest)** — parser para xls/csv anual de Damodaran, monitor de cuándo publica, schema nuevo. Trabajo medio. Útil long-term pero no blocking. **Extender la tabla v1** según aparezcan industries no cubiertas (ej. utilities específicos, consumer staples niche) en el flow de análisis de Joseda.

---

## Framework audit — Tier 2/3 deferred

Fuente: `Context/framework-alignment-audit.md` (2026-04-18). Tier 1 ya shipped (V1, V2a, V2c, V5). V2b obsoleto por SEC integration. Lo que queda:

- **V3 — Hide Valuation section for Mediocre/Poor tier businesses.** Hoy la Valoración sigue renderizándose aunque la empresa sea Poor y no tenga sentido valorarla. Pequeño cambio de UI condicionada al tier.
- **V4 / Q4 — Bank tier algorithm over-strict on 6-dimension path.** Una empresa financiera con 5 strong + 1 acceptable nunca llega a Exceptional (requiere ≥5 strong = applicable-1). Calibración: aflojar para 6-dim path o proporcional. Afecta bancos / aseguradoras / mortgage REITs.
- **V6 — "Bear / Base / Bull" label overclaims confidence.** La label sugiere más certidumbre de la que el rango realmente tiene. Rename o matizar copy.
- **Q1 — Add mortgage REITs to HARD_INDUSTRIES.** Spread-on-leveraged-MBS es apuesta de tipos de interés, no de negocio. Buffett explícitamente no invierte ahí. Hoy mREITs corren por balance-sheet-businesses; debería además disparar too-hard gate.

---

## Phase 6 signals pending

Event-driven review loop empezó con SEC cron (step 1-2 shipped 2026-04-20). Lo que queda:

- **Revisión anual deliberada.** Cron detecta posiciones cuya primera compra fue hace ~365d sin re-análisis en ese período → emite signal `annual_review_due`. Refuerza el ritual, reusa toda la infra de signals. Scope pequeño.
- **DEF 14A — proxy statements.** Compensación ejecutiva, related-party transactions, shareholder proposals. Anual por ticker, menos accionable que 13F/Form 4 pero complementario. Scope medio.
- **News APIs fase 2.** Marketwatch / Reuters / financial press. Deferred deliberadamente por recomendación del clean-mind agent: dogfood SEC-only durante 3-4 semanas primero para ver si sentimos el gap.

---

## Phase 7 Discovery extensions

Phase 7 Sessions 1-3 + cron semanal 13F + cross-signals shipped. Pendiente:

- **Form 4 coverage para los 860 tickers del roster Discovery.** Hoy Form 4 solo cubre los tickers del usuario. Extender a Discovery exige ingesta adicional (860 tickers × Form 4s diarios). Decisión: evaluar tras 3-4 semanas usando Form 4 sobre cartera propia.
- **Form 4 overlay en Discovery leaderboard.** Badge per-row tipo "3 insiders comprando últimos 90d" cuando extendamos la cobertura.
- **OpenFIGI API key.** Hoy free tier 25 req/min; con key 60 req/min. Relevante solo si hacemos backfill grande o extendemos Form 4 a Discovery. ~15 min de setup.

---

## Quality criteria presets

**Status:** arquitectura preparada (2026-04-27), implementación deferida hasta que tenga sentido productizar.

**Decisión:** si Moatboard sale de personal-tool, los criterios de tier (qué es Exceptional / Good / Mediocre / Poor) deberían ser **presets cerrados** elegibles por el usuario, no sliders abiertos. La razón: Moatboard tiene voz fuerte (Buffett-Munger-Smith-Akre); sliders abiertos diluyen esa voz. Presets ofrecen variedad sin perder opinión.

**Presets candidatos** (todos primary-source-anchored):
- `moatboard_default` — el composite actual (umbrales por dimensión + reglas de tier en `scorecard.ts` + `verdict.ts`). Único preset shipped.
- `buffett_classic` — Buffett 1987 letter (Fortune screen ROE>20% + no <15% en 10y, debt low, simple business). Más estricto.
- `akre_quality` — Akre three-legged stool (ROIC sostenido + reinvestment runway + management). Pondera más capital allocation.
- `smith_growth` — Terry Smith *Investing for Growth* (gross margin estable, FCF margin >20%, ROCE alto, no acquisitions de hostia). Pondera más cash conversion.
- `sleep_monopoly` — Nick Sleep Nomad (cost-leaders compartiendo escala con clientes). Solo aplica a un puñado.

**Arquitectura ya en sitio:**
- `discovery_pre_analyses.tier_preset` (default `'moatboard_default'`) hace explícito qué interpretación produjo el tier persistido.
- `scorecard_summary` JSONB ya guarda los datos crudos suficientes (`median`, `worstYear`, `quality`, `latestValue`, `yearsUsed` por dimensión) para recomputar tier desde otro preset sin re-correr el pipeline 10-K + AI.
- Los datos de Capa 1 (scorecard, moat, red flags) son user-agnostic. Solo Capa 2 (tier + applicable_dimensions gate + verdict prose) cambia entre presets.

**Cuando se implemente:**
1. Tabla `quality_criteria_presets` con thresholds + reglas (o hard-coded en `lib/presets/`).
2. Tabla `user_preferences` con `tier_preset` per-user.
3. Refactor `scorecard.ts` para aceptar preset (hoy hardcodea Moatboard default).
4. UI: selector compacto en /dashboard (Settings ligero).
5. La capa de re-evaluación per-ticker (10-K trigger en `runDailySignalsJob` + hook `upsertPreAnalysisFromExisting` en wizard) tendría que persistir una fila por preset activo en lugar de una sola, porque la opinión cambia con el preset aunque la data CAPA 1 sea la misma.

**No bloqueante hoy.** Personal-tool con un solo preset funciona perfecto.

---

## Ideas sin plan

Items que se mencionaron en alguna sesión pero no llegaron a plan formal:

- **Customer/supplier map de posiciones.** Para cada posición propia, identificar customers/suppliers públicos cotizados. Conecta Discovery con portfolio. Requiere LLM extraction de 10-K Item 1 + UX decision (¿dónde vive el grafo?). Alto esfuerzo, valor especulativo hasta probar.
- **Form 4 SELLS (code S).** Hoy solo code P. Las ventas son señal ambigua (diversificación, fiscalidad, 10b5-1). Pattern interesante: cluster selling (3+ insiders venden en 30d) puede ser materialmente informativo. No urge.
- **Aggregación de insider activity.** "3 insiders comprando este mes en AAPL" como vista mensual — `summarizeRecentInsiderPurchases` ya lo permite si hace falta.

---

## Killed with reason

Items considerados y decididos no construir, para no re-debatir:

- **Punch card surface (2026-04-22).** Widget contando moves/año. Descartado tras discusión: Moatboard ya está diseñado para no llevar al trading (sin precios, crons silenciosos, signals como revisión no acción). Un contador — incluso como espejo sin cap — es ornamental, no funcional. El sistema ya crea la disciplina por su propia naturaleza.

- **Mass agentic pre-tiering (2026-04-28).** Cron semanal corriendo Quality + Moat IA + Red flags IA sobre los 272 tickers candidatos del Discovery roster. Killed después de que la primera corrida consumiera todos los tokens Max con apenas 46 tickers procesados. Reemplazado por **shared cache poblado per-usuario al completar el wizard** (`upsertPreAnalysisFromExisting`) + **re-evaluación per-ticker en 10-K nuevo** (trigger en `runDailySignalsJob`). Misma cobertura efectiva (todos los tickers que algún usuario haya analizado quedan disponibles para los demás), coste IA proporcional al uso real, no al tamaño del universo.

- **In-product AI chat surfaces — business follow-up + valuation chat (2026-04-28).** El chat libre bajo el resumen de "Entender el negocio" (wizard Step 2 + ficha unificada Negocio tab) y la `valuation_chats` per-ticker conversacional (wizard `ValuationFollowupChat`, nunca renderizada en producción tras los cleanups del 2026-04-27). Killed por control de gasto IA: cada turno costaba un round-trip Sonnet/Opus sin techo claro, y el 99% del valor del entendimiento ya lo aporta el resumen + las 5-7 preguntas pregeneradas. Borrado: `FollowupChat.tsx`, `ValuationFollowupChat.tsx`, `valuationChats.ts`, `valuationChatAi.ts`, `askFollowupAction`, `askValuationFollowupAction`, `answerFollowupQuestion`, `appendFollowupQA`, `buildFollowupPrompt`. Schema: tabla `valuation_chats` retirada (drop en `init-db.mjs` + script one-shot `remove-ai-chats.mjs` para producción, que también filtra los Q&As `type='user_followup'` huérfanos del JSONB de `business_understanding`). El tipo `QnA` se estrechó a `type: 'pregenerated'` único. Mantenidos: análisis IA del negocio, preguntas tipo, red flags. Si en el futuro el dogfooding lo justifica, el chat se reactiva con un model más barato (Haiku) o cap diario per-user — no era una limitación arquitectónica.
- **Compound verdict en Valuation section (drift M, 2026-04-16).** Se consideró un tier de Valoración único compuesto; se descartó por la razón opuesta al punch card: compactar en un verdict único oculta el trabajo de weighing que el inversor debe hacer (latticework Munger). La solución es 4 herramientas independientes + AI Guide que pondera.

---

## Meta — cómo evolucionar este archivo

- Entries se pueden graduar entre secciones (Observed → Framework / Phase 6 / etc. cuando se clarifique el tamaño).
- Al matar un item, moverlo a **Killed with reason** con fecha + motivo para no re-proponerlo sin contexto.
- Si una sección se vacía, retirarla completamente (no dejar "(vacío)" permanente).
- Cuando un item se aborde en una sesión con Claude, linkar al commit o plan doc correspondiente al cerrarlo.
